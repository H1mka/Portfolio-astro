---
interface Props {
  title: string,
  subtitle?: string
}

const { title, subtitle } = Astro.props as Props
---

<div class="flex flex-col items-center gap-4 mb-10">
  <div class="px-5 py-1 bg-gray-200 rounded-xl">{title}</div>
  <div>{subtitle}</div>
</div>