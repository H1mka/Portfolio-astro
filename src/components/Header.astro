---
const links = [
  {name: 'About', url: '/'},
  {name: 'Work', url: '/'},
  {name: 'Contact', url: '/'}
]
---

<header class="px-20 flex justify-between items-center min-h-16 font-semibold z-10 relative">
  <h2 class="text-2xl">{'<Logo />'}</h2>

  <div class="right-menu flex gap-4 items-center">
    <nav>
      <ul class="list-none flex gap-6 text-gray-600 text-lg">
        {links.map(link => 
          <li>
            <a href={link.url}> {link.name} </a>
          </li>
        )}
      </ul>
    </nav>
    <div class="h-6 bg-black w-px"></div>
    <button class="bg-gray-900 text-white px-4 py-2 rounded-xl">Download CV</button>
  </div>
</header>